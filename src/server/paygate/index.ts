// Minimal stub for paygate integration
// All functions are intentionally no-op or return empty/neutral values so the app can build

export async function provisionPayGateForUser(userId: string): Promise<void> {
  // Intentionally empty: payment provisioning removed
  return;
}
